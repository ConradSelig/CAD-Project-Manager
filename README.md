Usage: projman.ps1 [command] [args] [-v] [-h]

Commands:
  newproject [project name]  Create a new FreeCAD project with Git and LFS.

Options:
  -v                        Enable verbose output.
  -h                        Display this help message.

Example:
  projman.ps1 newproject MyProject -v